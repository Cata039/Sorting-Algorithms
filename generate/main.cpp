#include "random.h"
#include "almost.h"
#include "reverse.h"

int main() {
    // Call functions to generate random, almost sorted, and reverse sorted numbers
    generateRandomNumbers();
    generateAlmostSortedNumbers();
    generateReverseSortedNumbers();

    return 0;
}
